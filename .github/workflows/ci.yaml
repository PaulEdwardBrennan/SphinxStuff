name: "Generate Documentation"
on:
- push

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - run: mkdir "/tmp/sphinx-log"
    - uses: ammaraskar/sphinx-action@master
      with:
        build-command: "sphinx-build docs build"
        docs-folder: "docs/"